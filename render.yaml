services:
  - type: web
    name: ai-vibe-proxy
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: VITE_GEMINI_API_KEY
        sync: false
      - key: NODE_VERSION
        value: 18.0.0
